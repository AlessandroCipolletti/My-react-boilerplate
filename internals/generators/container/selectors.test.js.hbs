import { select{{ properCase name }}Domain, makeSelect{{ properCase name }}Key } from '../selectors'

describe('{{ properCase name }} Selectors', () => {
  it('select{{ properCase name }}Domain should select the global page state', () => {
    const {{ camelCase name }}State = {}
    const mockedState = {
      {{ camelCase name }}: {{ camelCase name }}State,
    }

    expect(select{{ properCase name }}Domain(mockedState)).toEqual({{ camelCase name }}State)
  })

  it('makeSelect{{ properCase name }}Key should select the {{ camelCase name }}.key state', () => {
    const {{ camelCase name }}State = {
      key: 'value',
    }
    const keySelector = makeSelect{{ properCase name }}Key()

    expect(keySelector({{ camelCase name }}State)).toEqual({{ camelCase name }}State.key)
  })
})
