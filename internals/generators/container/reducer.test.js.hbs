import produce from 'immer'
import {{ camelCase name }}Reducer from '../reducer'
import { defaultAction } from '../actions'

/* eslint-disable default-case, no-param-reassign */
describe('{{ camelCase name }}Reducer', () => {
  let state
  beforeEach(() => {
    state = {
      // default state params here
      key: 'value',
    }
  })

  it('returns the initial state', () => {
    const expectedResult = state
    expect({{ camelCase name }}Reducer(undefined, {})).toEqual(expectedResult)
  })

  it('should handle the defaultAction action correctly', () => {
    const expectedResult = produce(state, draft => draft)
    expect({{ camelCase name }}Reducer(state, defaultAction())).toEqual(expectedResult)
  })
})
